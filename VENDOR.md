# Vendored libraries

`vendor/ical.js`:

- Version: 2.2.1
- URL: https://cdn.jsdelivr.net/npm/ical.js@2.2.1/dist/ical.js
- License: MPL-2.0

The build copies this file byte-for-byte from the exact npm release declared in
`package.json` and `package-lock.json`. `npm run validate` verifies its contents.